const Discord = require("discord.js");
const config = require("../../config.json");

module.exports.run = async (bot, msg, args) => {

}

module.exports.help = {
    name: 'promote',
    role: 'we\'ll get back to you',
    usage: '`User`',
    desc: `Promotes mentioned user`,
    example: '!promote @That "One" Turtle#1123'
}